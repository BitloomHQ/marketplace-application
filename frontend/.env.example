# API base URL (optional). Leave empty to use Vite proxy (/api -> http://127.0.0.1:8000)
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Google Maps key lives on the backend only — see project root .env (GOOGLE_MAPS_API_KEY)
