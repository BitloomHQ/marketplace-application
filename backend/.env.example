# Copy to backend/.env and fill in real values.
# Loaded by backend/backend/settings.py (required unless noted).

# Django
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database (PostgreSQL)
DB_NAME=home_services_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# Redis (Django Channels / WebSockets)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Optional — server-side only; never expose as VITE_* in the frontend.
# Enable Places API (New) and Geocoding API in Google Cloud.
GOOGLE_MAPS_API_KEY=

# Email (Resend) — https://resend.com/api-keys
# Use a verified domain/sender in DEFAULT_FROM_EMAIL for production.
RESEND_API_KEY=
DEFAULT_FROM_EMAIL=Marketplace <onboarding@resend.dev>
